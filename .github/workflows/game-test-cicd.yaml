name: '(Dev) Game test ci-cd'

on: workflow_dispatch

jobs:
  development-deploy:
    runs-on: 'ubuntu-latest'
    if: ${{ $GITHUB_REF_NAME == 'develop' }}

    steps:
    - uses: 'actions/checkout@v4'

    - run: echo "Deploying ${{ $GITHUB_REF_NAME }}"
